{% set taxlist = children_only is defined ? taxonomylist.getChildPagesTags() : taxonomylist.get() %}

{% if taxlist %}
<span class="tags">
	
   <!-- hard-coding tags
   		{% for tax,value in taxlist[taxonomy] %}
        {% set label_class = uri.param(taxonomy) == tax ? 'label-primary' : 'label-secondary' %}
        <a class="label label-rounded {{ label_class }}" href="/{{ taxonomy }}{{ config.system.param_sep }}{{ tax }}">{{ tax }}</a>
    {% endfor %}
     -->
    <a class="label label-rounded label-secondary" href="http://www.couldbearchitecture.com"> ALL </a>
    <a class="label label-rounded {{ label_class }}" href="/tag:building#grid">building</a>
 	<a class="label label-rounded {{ label_class }}" href="/tag:thinking#grid">thinking</a>
 	<a class="label label-rounded {{ label_class }}" href="/tag:teaching#grid">teaching</a>
    <a class="label label-rounded {{ label_class }}" href="/tag:writing#grid">writing</a>
    
{% endif %}
